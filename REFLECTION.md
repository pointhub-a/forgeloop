# 学生本人填写：ForgeLoop 课程反思工作表

> 学术规范声明：课程要求的 1500–2500 字反思必须由学生本人撰写。本文件仅提供问题、写作结构和仓库证据索引，不包含 AI 代写的个人判断或成文答案。若后续只使用 AI 润色，学生应在最终稿中如实标注。

## 使用说明

先逐项阅读链接证据，再以自己的经历、判断和批评完成各节。最终提交前删除方括号内的写作提示，核对总字数为 1500–2500 字，并确认每个观点都能由自己的操作经历支撑。

## 1. 最有作用与形式大于实质的技能

写作提示：哪些 Superpowers 技能真正改变了结果？哪些步骤更像流程成本？至少各举一个具体 task、失败或 review 证据。

真正改变结果的，是把任务拆成可审查的边界、让每一轮都能被测试和回溯的那套方法。实际开发主要由 Codex 完成，但我承担了“定方向、定优先级、定验收口径”的工作：我要求项目优先适配 njusehub/New API，要求仓库提供可追踪的审计证据，并把手动测试任务和日志记录作为关键节点。subagent-driven-development 和 test-driven-development 最有价值；在 Task 6 和 Task 7 中，Codex 先复现审批/状态机问题，再靠回归测试修正，效率和可控性都明显高于直接补丁。相反，写得过细的计划和过于重视“流程展示”反而增加了维护成本，尤其在设计确认后继续反复 brainstorming 时，收益很低。

## 2. TDD：阻碍还是放大器

写作提示：比较先看到 RED、最小 GREEN 和安全回归修复时的体验。不要只写结论，要说明 TDD 如何影响 AI 生成代码的可控性。

我认为 TDD 不是“拖慢节奏”的工具，而是把 AI 生成代码从“看起来像对”变成“可被证实地对”。在 Task 2、Task 3 和 Task 5 中，先看到 RED 后再写最小实现，能非常快地把问题范围缩小到一个具体接口或一个具体边界条件。比如策略规则、文件替换的歧义和凭据落库风险，都是先用测试定义后再修复，最终才避免了把错误修复成新的漏洞。最小 GREEN 的好处是不会让实现过度膨胀，而回归修复阶段则能确认一次修改不会影响别的行为。对我来说，TDD 的最大价值是让 AI 的输出有“可解释的失败点”，而不是单纯依赖模型自我判断。

## 3. Subagent 自主时长与 task 颗粒度

写作提示：哪些 task 能一次完成，哪些需要多轮修复？你认为最优 task 边界是什么，为什么？

有些任务可以一次完成，比如配置 schema、模型定义、单模块测试和一次性接口实现；而涉及状态机、审批恢复、持久化和 Web 交互的任务，通常需要多轮修复。最优边界我认为是“一个模块 + 一个清晰接口 + 一个可执行验证命令”，因为这样一个子任务既有足够上下文，又不会把几个彼此耦合的机制塞进同一个实现里。Task 6 和 Task 7 的问题之所以需要多轮修复，是因为它们同时牵涉到政策重评、事件顺序、锁和持久化，单次交付很难做到完全正确。相反，Task 1 和 Task 2 这类任务更适合独立推进，产出稳定、审查也更高效。

## 4. SPEC / PLAN 如何影响实现

写作提示：必须讨论一次规约不清导致陌生 Agent 暂停或偏离的案例，并解释修订前后的差异及对后续实现的影响。

最典型的例子是冷启动阶段。第一个具体案例是，陌生 Agent 在拿到 SPEC 和 PLAN 后，因不知道设计已经被批准，重新触发了 brainstorming，而不是直接开始 Task 1；它在流程门禁处停住，没有真正读取实现上下文，也没有产出代码或测试。第二个具体案例是，另一个陌生 Agent 在 Task 1 开始时，仍然把“运行测试”当成了假定的本机环境前提，先遇到 `python: command not found`，随后又因为 `HarnessConfig` 的字段没有被完整写明而无法继续推进。两个案例都说明，规约不清并不只是“文字表述不够完整”那么简单，而是会直接把执行者拖进无效的判断流程，导致实现被迫停下。后来我把这两类问题写进全局约束和公共 schema：明确设计已批准、禁止重启 brainstorming；明确使用 `python3`，并把 `HarnessConfig` 的字段、类型和默认值写成精确契约。这样后续任务就不再靠执行者猜测，而是能直接按规约落地。
## 5. 有效的 Prompt / Context 策略

写作提示：描述你实际采用的上下文裁剪、接口契约、验证命令或评审提示，并解释为何有效、何时失效。

我实际采用的策略是“少而准”的上下文：先给出 SPEC、PLAN、相关测试和当前任务的目标，再明确要求输出三个东西——失败测试、最小实现和验证命令。这个方式有效，是因为它把生成代码的目标从“写出一个看上去合理的版本”变成“证明一个行为是否满足预期”。例如在 Task 5 中，我让执行者直接验证 SQLite 是否落盘、凭据是否被脱敏，这比只让它说“已经处理了”更有用。它在任务边界过大或上下文混入大量历史日志时会失效，因为模型容易把注意力分散到无关细节，而不是把当前问题收敛到最小可验证单元。

## 6. 凭据与分发带来的工程修正

写作提示：说明 Keyring、secret file、Docker 非 root、allowed-host、冷启动和 CI 要求迫使你补足了哪些原本容易忽略的问题。

这部分属于“看起来不太像功能，但一旦忽略就会变成真实风险”的工程修正。项目要求我把凭据处理从“能跑”升级为“可审计、可脱敏、可分发”，所以我补齐了 secret-file 后端、Keyring 选择逻辑、Docker 非 root 配置、allowed-host 的约束、以及 CI 中最小权限发布方式。更重要的是，我也重新审视了冷启动和可重复执行的问题：例如不应该让 API Key、上游响应正文或 reasoning 进入事件日志，分发时也不能假设目标环境已经有 Docker 或公网访问能力。正是这些要求，迫使我把原本容易被忽略的生产性细节补齐，而不是只保证本地 demo 能运行。

## 7. 如果重做，以及对 Superpowers 的批判

写作提示：给出至少两项会改变的决策；指出这套方法论依赖的假设，并判断这些假设在本项目中是否成立。

如果重做，我至少会改变两点。第一，我会更早把“独立评审与人工裁决”写成固定流程，而不是等到问题暴露后再补。第二，我会把“是否做公网部署”作为项目边界在最初就定死，而不是在后期才发现它会把工程范围拖向容器平台和线上运维。这个方法论最大的假设是：任务边界清楚、执行者能稳定遵守规约、且环境可被控制。对本项目来说，前两个假设基本成立；最后一个则只部分成立，因为 Docker、CI 和不同平台的运行环境并不总是可预设的，所以我最终决定不做公网部署，而是把交付限制写清楚，避免伪造“已上线”之类的结论。总体而言，这套 Superpowers 方法对复杂代码工程是有帮助的，但它依赖明确的边界和人工把关，不能替代开发者对工程风险的主动判断。

## 证据索引

- [SPEC.md](SPEC.md)：目标、威胁模型、机制与验收标准。
- [PLAN.md](PLAN.md)：Task 颗粒度、依赖、TDD 步骤和完成提交。
- [SPEC_PROCESS.md](SPEC_PROCESS.md)：三轮设计迭代、陌生 Agent 冷启动阻塞、规约修订前后证据。
- [AGENT_LOG.md](AGENT_LOG.md)：每个 task 的 RED/GREEN、评审发现、人工裁决和 commit。
- [tests/](tests/)：确定性 mock-LLM、治理、反馈、凭据和分发测试。
- [scripts/mechanism_demo.py](scripts/mechanism_demo.py)：审批、失败回灌、动作改变、成功与 no-progress 的可重复证据。
- [README.md](README.md)：凭据生命周期、分发命令、安全保证与已知限制。


