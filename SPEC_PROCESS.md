# SPEC 与 PLAN 协作过程

> 本文记录真实过程。后续冷启动验证和实现阶段将持续追加。

## 1. Brainstorming 关键节点

### 迭代一：从“做一个 Harness”收敛主要贡献

智能体首先区分了四个可深入方向：反馈闭环、安全治理、工具扩展和记忆/上下文。使用者未指定领域细节，智能体基于课程“机制必须是代码”和可离线测试的判据，推荐反馈闭环为主、治理为辅。

采纳：测试/lint/类型检查作为客观反馈；危险动作代码拦截；Mock LLM 驱动确定性演示。
放弃：多 Agent 编排和向量记忆。原因是它们扩大范围，但不能增强最核心的“行为是否正确”证据。

### 迭代二：从 CLI 内核扩展到可交付产品

补齐通用要求后，发现 WebUI 和线上 URL 是最终清单硬性项。智能体将轻量 Web 控制台纳入系统，提供任务轨迹、验证报告和人工审批，同时明确 WebUI 不是主要研究贡献，避免前端挤占机制实现时间。

修正前：纯 CLI/后端 Harness。
修正后：核心仍可脱离 Web 运行，但额外提供薄 Web/API 层。

### 迭代三：安全、凭据和分发约束

通用要求迫使设计不只考虑 API Key 的“读取”，而是完整覆盖隐藏录入、状态查看、更新、清除和目标机器配置。最终选择本机 Keychain + 容器 secret 文件，拒绝把 `.env` 当作默认安全存储。

同时识别出要求中的 GitHub Actions 与 GitLab CI 冲突。决策是以最终交付清单为准：实现 `.gitlab-ci.yml` 的 `unit-test` job；GitHub Actions仅作可选补充。

## 2. AI 建议与人工授权

AI 提出并采用：反馈指纹、无进展检测、动作绑定审批、工作区符号链接围栏、Mock scripted provider、服务端渲染 WebUI。使用者在阅读方向与完整要求概括后明确授权“你来做”，并要求尽快形成可交付版本。

由于使用者授权持续自主推进，书面 SPEC 的逐段文字确认由主智能体自检和陌生智能体冷启动审查代行；最终提交前仍应由使用者本人快速阅读和签字。这一偏离会同步记录在 `AGENT_LOG.md`。

## 3. Brainstorming 反思（阶段性）

做得好：强制区分“LLM 能力”和“Harness 代码机制”，并用离线可测性压缩了模糊功能。
不足：逐节等待人工确认适合协作设计，但与“夜间自主产出”的目标存在张力。本项目通过提前展示完整方案、获得总体授权和保留复核门禁来折中，而不是省略设计。

## 4. 冷启动验证

### 第一次尝试：流程门禁阻塞

全新子 Agent 被放入 `coldstart/spec-validation` worktree，只允许依据 SPEC 与 PLAN 尝试 Task 1。它首先自动载入 Superpowers 流程技能，并因 `brainstorming` 的设计批准门禁而停留在流程判断，未在限定时间内读取 SPEC/PLAN，也未写代码。

暴露缺陷：计划虽由已批准 SPEC 生成，但没有在执行者入口处显式写明“设计已批准，不要重新 brainstorming”。这会让完全陌生的 Agent 将实现任务误判为新的创意工作。

修订决策：在后续派发中明确该上下文，并在 PLAN 全局约束中固定这一点；随后使用另一个无对话历史的 Agent 重试。当前机器只安装了 Codex，没有第二种 Coding Agent 客户端，因此未能满足“类型不同”；这一限制不隐藏，提交前仍建议由 Claude Code、Gemini CLI 或其他类型 Agent 再做一次规定时长的冷启动。

### 第二次尝试：实际红测试暴露环境假设与 schema 歧义

第二个无对话历史 Agent 完整读取 SPEC/PLAN，并创建 `tests/test_models.py`。它运行计划指定的 `python -m pytest tests/test_models.py -q`，得到 `python: command not found`，而不是预期的模块导入失败。这证明计划把开发机的 Python 别名当成了隐含前提。

它随后在 Task 1 停止，因为“从 SPEC 复制 HarnessConfig 字段”无法落实：原 SPEC 只列配置类别，没有给出完整字段名、类型与默认值。该歧义位于公共 schema，猜测后会影响所有后续模块。

修订前：`HarnessConfig fields copied from SPEC`，裸命令使用 `python`。
修订后：SPEC §5.7 给出 16 个配置字段及精确类型/默认值；计划逐项复制这些值；裸机命令统一使用 `python3`。

冷启动分支未合并，因为其作用是验证规约，而不是提供正式实现。
